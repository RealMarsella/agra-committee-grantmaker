// Stub for optional wagmi connector dependencies (e.g. the Tempo "accounts"
// SDK) that we never instantiate. Aliased in next.config so the bundler can
// resolve the bare dynamic import without pulling in an uninstalled package.
export {};
