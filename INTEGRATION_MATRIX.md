# Integration Matrix

Hackathon: Agora Agents Hackathon
Idea: AGRA Committee-Governed Grantmaker
Hardening updated: 2026-05-22 05:26 IST

| Dependency | Real access path | Env / credential | Proof command or browser proof | Fixture policy | Status | Blocker |
|---|---|---|---|---|---|---|
| Next.js local app/API | `http://localhost:3003` via `npm run start -- -p 3003` | none | `GET /api/applications`, `POST /api/applications`, `POST /api/replay`, malformed JSON 400 all verified by curl | No fixture label needed for API mechanics; data remains demo fixture unless submitted in current process | proven local | In-memory process store only. |
| Public deployed app | `https://agra-committee-grantmaker.vercel.app` | Vercel auth already configured locally | `npx vercel deploy --prod --yes` -> READY `dpl_FuyoeN8fSxmgHkNYrrD82d8Aa1pj`; `curl -I -L` -> HTTP 200; browser expect passed title + `Demo intake` | UI must label fixture Arc write proof | proven public demo | Not submit-ready without video, traction, live Arc write. |
| GitHub source repo | `https://github.com/gabrielantonyxaviour/agra-committee-grantmaker` | local `gh` auth | `gh repo view ...` returned `visibility: PUBLIC`, `isPrivate: false`; `origin` remote matches URL | Repo claims must not imply final submission | proven public repo | Hardening changes require commit/push if the repo should mirror local artifacts. |
| Arc Testnet RPC | `https://rpc.testnet.arc.network` | `ARC_TESTNET_RPC_URL`, defaults to public RPC | `npm run arc:check` returned chain ID `5042002` | None for read proof | proven read-only | None for reads. |
| Arc USDC | ERC-20 `0x3600000000000000000000000000000000000000` | public RPC only for decimals; funded wallet needed for writes | `npm run arc:check` returned USDC decimals `6` | Any payment/tx claim must remain fixture until broadcast | proven read-only / write blocked | Test wallet balance is `0`. |
| Arc EURC | ERC-20 `0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a` | public RPC only | `npm run arc:check` returned EURC decimals `6` | EURC may appear as gated option only | proven read-only | No EURC transfer proof. |
| Local test wallet | `AGRA_TEST_WALLET_ADDRESS` in untracked `.env.local` | private key must stay untracked and unprinted | `npm run arc:check` reported wallet `0x58374c7ec9192e2d588e39ACA0eA43f60f432b13`, native USDC gas balance `0`, `canBroadcast: false` | Wallet proof can be shown; private key never included | blocked for writes | Needs testnet USDC/faucet. |
| Circle faucet | `https://faucet.circle.com` through `agent-browser`/Gabriel profile from prior builder run | profile session only; no credential committed | Builder evidence: faucet displayed unusual-traffic / verification challenge; screenshots under `outputs/faucet-*.png` | No fake funding claim allowed | blocked | CAPTCHA/unusual-traffic challenge and no funded wallet. |
| DecisionRegistry contract | Local Foundry + future Arc deployment | `DECISION_REGISTRY_ADDRESS` only after deploy | `forge test` passed 2 tests; `npm run replay` produced encoded `recordDecision` calldata | Registry address/tx hash stays fixture or ready until deployment proof exists | local contract proven / live deploy blocked | Needs funded wallet and deployment tx. |
| Replay broadcast | `npm run replay:broadcast` | `ARC_PRIVATE_KEY` + `DECISION_REGISTRY_ADDRESS` | `npm run replay` skipped broadcast and printed fixture proof; `arc:check` says `canBroadcast: false` | Fixture proof must say no Arc transaction exists | blocked | Missing funding and deployed registry. |
| Google submission form | `https://forms.gle/ok3Gr9zhmHnApvK48` | Gabriel profile if browser-assisted | Builder inventoried fields; no fields prefilled; no legal/final submit | Portal copy must not claim video/traction/tx until provided | inventoried only | Missing video URL and real traction answers; final submit requires explicit approval. |
| M2 formal `/polish` | `playwright-cli-sessions` via `PLAYWRIGHT_CLI_REMOTE=m2worker` | SSH/Tailscale to `m2worker` | `npx playwright-cli-sessions browser start` failed: SSH to `100.115.214.82:22` timed out; report saved to `~/.playwright-sessions/.reports/2026-05-21T23-47-55-644-agra-hardening-m2-formal-polish-preflight-failed.md` | Cannot call formal polish passed | blocked | M2 worker unreachable. |
| Local fallback visual QA | Local headless Chrome via `env -u PLAYWRIGHT_CLI_REMOTE npx playwright-cli-sessions@latest exec scripts/local-visual-qa.mjs` | none | Passed at 375/768/1440; no horizontal overflow; no browser errors; nonblank canvas; screenshots under `outputs/visual-qa-hardening/` | Label as local visual QA, not formal polish | proven fallback | Formal M2 gate still blocked. |
